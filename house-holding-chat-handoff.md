# House Holding 引き継ぎ用チャット分類

このファイルは、家計簿アプリでこのチャット中に決めた内容を、House Holdingプロジェクトへ分類別に引き継ぐためのものです。
各分類をHouse Holding内で1チャットとして作成し、「初回メッセージ」をそのまま貼り付けてください。

## 1. 入出金に関する操作

### チャットタイトル
入出金に関する操作

### 初回メッセージ
家計簿アプリの入出金操作を引き継いでください。

現在の仕様は以下です。
- 財布の入出金を入力できる。
- 口座の収入・支出を入力できる。
- 財布の入出金ではカテゴリ欄を使わない。
- 新しい支出を入力するときはカテゴリ欄をリセットする。
- `１ヶ月分（メイン）`の出金は、標準で財布へ入る。
- 財布への入金時は、入金元を口座または「その他」から選べる。

今後の検討では、入力の手軽さと、あとから履歴を見返したときの分かりやすさを優先してください。

### 参照元
- `app.js`: 財布入出金、口座支出、財布入金処理
- `index.html`: 財布入出金ダイアログ、口座入出金フォーム

## 2. 固定収入・固定支出に関する操作

### チャットタイトル
固定収入・固定支出

### 初回メッセージ
家計簿アプリの固定収入・固定支出まわりを引き継いでください。

現在の仕様は以下です。
- 固定収入と固定支出を分けて設定する。
- 固定収入選択時の追加ボタンは「固定収入を追加」。
- 固定支出選択時の追加ボタンは「固定支出を追加」。
- 毎月指定した日に、アプリ内の口座残高へ反映する。
- 次月分などを月末に前倒し入力できる。
- 固定収支は削除できる。

今後の検討では、「いつの月の分か」と「実際にお金が動いた日」を混同しない設計を優先してください。

### 参照元
- `app.js`: 固定収支生成、指定日反映、前倒し入力、削除処理
- `index.html`: 固定収支フォーム、前倒し入力フォーム

## 3. 振込に関する操作

### チャットタイトル
振込機能

### 初回メッセージ
家計簿アプリの振込機能を引き継いでください。

現在の仕様は以下です。
- 口座間振込ができる。
- 自動振込を設定できる。
- 振込は、出金側と入金側をペアで記録する。
- 月リセットや削除時には、関連する振込ペアも連動して処理する。

今後の検討では、二重計上を避けることと、履歴上で振込だと分かる表示を優先してください。

### 参照元
- `app.js`: `createTransferPair`、自動振込生成、振込削除・月リセット処理
- `index.html`: 口座間振込フォーム、自動振込フォーム

## 4. 口座・財布管理に関する操作

### チャットタイトル
口座・財布管理

### 初回メッセージ
家計簿アプリの口座・財布管理を引き継いでください。

現在の仕様は以下です。
- 口座は2つ。
- 口座名は `１ヶ月分（メイン）` と `貯金`。
- 財布の残金を表示する。
- 財布の残金を編集できる。
- 口座名と口座残高も編集できる。
- 総資産ページで財布・口座をまとめて管理する。

今後の検討では、現在どの口座を見ているかが一目で分かることを優先してください。

### 参照元
- `app.js`: 初期口座名、財布残高、口座編集、財布編集
- `index.html`: 残高カード、口座編集ダイアログ、財布編集ダイアログ

## 5. 月次管理に関する操作

### チャットタイトル
月次管理とリセット

### 初回メッセージ
家計簿アプリの月次管理とリセット機能を引き継いでください。

現在の仕様は以下です。
- 月ごとに記録を管理する。
- 月ごとにリセットできる。
- リセット時は、その月の取引、固定収支、振込、財布連動分を戻す。
- スマホでもリセットボタンが表示されるよう調整している。

今後の検討では、リセット対象がどこまでかを明確にし、意図しない削除を避ける確認設計を優先してください。

### 参照元
- `app.js`: 月次リセット処理
- `index.html`: リセットボタン、古いページ向けフォールバック
- `sw.js`: 古いキャッシュ向けリセットボタン補助

## 6. 表示・分析に関する機能

### チャットタイトル
表示と分析

### 初回メッセージ
家計簿アプリの表示・分析機能を引き継いでください。

現在の仕様は以下です。
- その月の収入を円グラフで表示する。
- その月の支出を円グラフで表示する。
- 収入合計、支出合計、固定収入、固定支出、振込差額を表示する。
- 月ごとの取引履歴を表示する。

今後の検討では、グラフの見やすさよりも、まず金額と分類の誤解が起きないことを優先してください。

### 参照元
- `app.js`: 月次集計、円グラフ描画、取引履歴表示
- `index.html`: グラフ、月次サマリー、取引履歴エリア

## 7. UIに関する変更

### チャットタイトル
UIとデザイン

### 初回メッセージ
家計簿アプリのUI・デザイン変更を引き継いでください。

現在の仕様は以下です。
- トップ画面は、財布の残金と財布入出金ボタンを中心に表示する。
- `総資産`ページを作り、口座管理をそちらへ移動している。
- ガラス感・透明感のあるデザインにしている。
- スマホ表示に対応している。
- 口座選択ボタンと残高表示の形を分けている。
- 口座ごとにテーマカラーを変える。
  - `１ヶ月分（メイン）`: 緑系
  - `貯金`: 青系

今後の検討では、見た目の装飾よりも、現在位置と操作対象が分かることを優先してください。

### 参照元
- `styles.css`: ガラス感、スマホ対応、口座テーマカラー
- `app.js`: 口座別テーマ切替
- `index.html`: 財布ページ、総資産ページ

## 8. ダイアログ・入力体験の修正

### チャットタイトル
ダイアログと入力体験

### 初回メッセージ
家計簿アプリのダイアログと入力体験の修正内容を引き継いでください。

現在の仕様は以下です。
- 財布の入出金ダイアログが閉じられない問題を修正済み。
- 財布編集画面を口座編集画面に近い形式へ変更済み。
- 日付入力はクリックするとカレンダーが出る形式。
- 入力後に不要な欄をリセットする。

今後の検討では、スマホで片手操作しやすいことと、入力後に前回値が残って誤入力しないことを優先してください。

### 参照元
- `index.html`: 財布入出金ダイアログ、財布編集ダイアログ、日付入力
- `app.js`: ダイアログ表示、フォームリセット、保存処理

## 9. データ管理・共有に関する機能

### チャットタイトル
データ管理と共有

### 初回メッセージ
家計簿アプリのデータ管理・共有機能を引き継いでください。

現在の仕様は以下です。
- JSONエクスポートができる。
- JSONインポートができる。
- GitHub tokenを使い、リポジトリ内の `data/household-data.json` に保存・読込できる。
- データは基本的に端末ごとのブラウザ保存。
- 公開リポジトリに実データを置くと第三者が見られる可能性がある。

今後の検討では、実データを扱う場合はprivateリポジトリまたは別の安全な保存先を優先してください。

### 参照元
- `README.md`: データ保存・共有に関する注意
- `index.html`: JSONエクスポート、インポート、GitHub保存UI
- `data/household-data.json`: GitHub保存先

## 10. スマホアプリ化・公開まわり

### チャットタイトル
PWAと公開設定

### 初回メッセージ
家計簿アプリのスマホアプリ化・公開まわりを引き継いでください。

現在の仕様は以下です。
- PWA対応済み。
- `manifest.webmanifest` を追加済み。
- アプリアイコンを追加済み。
- `sw.js` によるオフライン・キャッシュ対応済み。
- GitHub Pagesでスマホから開ける想定。
- 古いキャッシュが残る場合に備えて、`sw.js` 側に一部補助処理が入っている。

今後の検討では、GitHub Pagesのキャッシュ遅延やPWA更新タイミングを前提に、ユーザーへ再読み込み・開き直しの案内を用意してください。

### 参照元
- `README.md`: PWA利用手順
- `manifest.webmanifest`: PWA設定
- `sw.js`: キャッシュ、オフライン、古いページ向け補助
- `icons/`: PWAアイコン

